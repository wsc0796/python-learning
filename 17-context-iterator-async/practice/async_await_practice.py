"""
17-context-iterator-async: async/await 练习

TODO: 完成以下 6 个练习
"""

import asyncio
import time


# ============================================================
# TODO 1: 第一个 async 程序
# ============================================================

# 要求：
#   - 定义 async def hello()
#   - 打印 "Hello"
#   - await asyncio.sleep(1)
#   - 打印 "World"
#   - 用 asyncio.run(hello()) 执行


# ============================================================
# TODO 2: 对比同步和异步耗时
# ============================================================

# 要求：
#   - 写 3 个同步任务函数，每个 time.sleep(1)
#   - 写 3 个异步任务函数，每个 await asyncio.sleep(1)
#   - async def main() 中分别用顺序 await 和 asyncio.gather 执行
#   - 打印每种方式的总耗时


# ============================================================
# TODO 3: 使用 create_task 并发
# ============================================================

# 要求：
#   - 定义 async def download(name)，用 await asyncio.sleep(2) 模拟
#   - 在 main() 中用 create_task 创建 3 个任务
#   - 等待全部完成
#   - 预期总耗时约 2 秒（不是 6 秒）


# ============================================================
# TODO 4: 使用 gather 获取结果
# ============================================================

# 要求：
#   - 定义 async def fetch(id)，返回 f"data_from_{id}"
#   - 用 asyncio.gather() 并发执行 5 个 fetch()
#   - 打印结果列表


# ============================================================
# TODO 5: 异步异常处理
# ============================================================

# 要求：
#   - 定义一个异步函数，内部抛出 ValueError
#   - 在 main() 中用 try/except 捕获
#   - 测试 gather 中有一个任务失败的情况


# ============================================================
# TODO 6: 模拟异步 LLM API 调用
# ============================================================

# 要求：
#   - 定义 async def call_llm(prompt)，用 await asyncio.sleep(2) 模拟
#   - 返回模拟回答 f"LLM 回复: {prompt[:20]}..."
#   - 用 gather 同时调用 3 个不同的 prompt
#   - 打印结果列表和总耗时
