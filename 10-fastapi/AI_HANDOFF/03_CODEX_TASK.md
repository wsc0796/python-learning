# Codex 执行任务单

## 本次目标

待填写。

## 背景

当前项目是 `10-fastapi`，主线是：

```text
Pydantic -> Service -> Repository -> Depends -> FastAPI
```

## 允许修改范围

- 只允许修改：待填写
- 可以新增：待填写
- 不允许修改：待填写

## 架构边界

- 路由层负责：HTTP 请求、响应、状态码、异常转换。
- Service 层负责：业务逻辑和 CRUD。
- Repository / Model 层负责：数据读写、Pydantic 校验和序列化。
- 必须保持兼容的字段 / 接口：待填写。

## 业务规则

1. 待填写
2. 待填写
3. 待填写

## 伪代码

```pseudo

```

## 执行要求

1. 先检查项目结构和相关文件。
2. 先说明影响范围。
3. 小步修改。
4. 不做无关重构。
5. 完成后运行测试。

## 验收标准

- `python run_checks.py` 通过。
- 待填写。

## 输出要求

- 改了哪些文件。
- 每个文件为什么改。
- 跑了什么测试。
- 有哪些风险。
- 写入 `AI_HANDOFF/04_REVIEW.md`。

