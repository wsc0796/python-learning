# 01_REQUIREMENT

## 原始需求

这里记录你和 GPT 敲定的需求。当前先作为 `10-fastapi` 的交接区占位。

## 业务背景

- 当前项目：`10-fastapi`
- 当前主线：Pydantic 校验数据 -> Service 做 CRUD -> Repository 保存 JSON 文件 -> Depends 注入 Service -> FastAPI 暴露接口
- 当前验证命令：`python run_checks.py`

## 用户视角

在 `/docs` 或 TestClient 中完成任务的创建、查看、完成、删除。

## 边界问题

- 必须支持：围绕当前 JSON 版 CRUD 项目练习。
- 暂时不做：Django、Flask、Celery、Kubernetes、复杂微服务。
- 不确定点：每次新任务开始前补充。

## 我自己的判断

- 业务方向是否确认：
- 我能否解释这个需求：
- 是否可以进入方案设计：

