"""
Day 3 验收测试用例。
"""


TESTS = [
    ("task01", (3, 5), "应返回两个数的和"),
    ("task02", ("小明",), "应使用默认问候语"),
    ("task03", ("Tom", 80), "应返回姓名和分数"),
    ("task04", (1, 2, 3, 4), "应返回总和"),
    ("task05", (), "手动测试关键字参数输出"),
    ("task06", ((3, 4),), "应通过解包调用加法"),
    ("task07", ([('A', 80), ('B', 90)],), "应按分数排序"),
    ("task08", ([80, 59, 90],), "应调用多个小函数完成统计"),
]


if __name__ == "__main__":
    for name, args, check in TESTS:
        print(f"{name}{args}: {check}")

