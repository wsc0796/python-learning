# Day 2 程序阅读题

## 题 1（考点：切片，建议 4 分钟）

```python
s = "python"
print(s[1:4])
print(s[::-1])
```

你的预测：

## 题 2（考点：列表方法，建议 5 分钟）

```python
nums = [3, 1, 2]
nums.append(0)
nums.sort()
print(nums)
```

你的预测：

## 题 3（考点：字典 get，建议 6 分钟）

```python
d = {}
for ch in "aba":
    d[ch] = d.get(ch, 0) + 1
print(d)
```

你的预测：

## 题 4（考点：集合去重，建议 4 分钟）

```python
data = [1, 2, 2, 3, 1]
print(len(set(data)))
```

你的预测：

## 题 5（考点：推导式，建议 5 分钟）

```python
result = [x * 2 for x in range(5) if x % 2 == 0]
print(result)
```

你的预测：

